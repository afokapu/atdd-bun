schema_version: 1.0.0
rule_id: planner.train.interlocking-payload-contract-body-required
kind: rule
status: active
name: Interlocking payload contract body required
statement: A payload contract identity referenced by an interlocking message must
  resolve to a contract schema body.
terms:
- term_id: contract_body
  text: a payload contract identity must resolve to a contract schema body on disk
implementation:
  type: validator
  ref: test_sequence_diagram_sanity::test_payload_contracts_have_schema_bodies
metadata:
  severity: 3
  disposition: strict
validation:
  family: resolution
  template: artifact_reference_resolution
  variant: planner_train_interlocking_payload_contract_body_required
  phase: CONFIRM
  enforcement: confirm-blocking
  subject_kind: interlocking
  failure_evidence:
  - interlocking_id
  - message_id
  - contract
  - expected_schema_path
