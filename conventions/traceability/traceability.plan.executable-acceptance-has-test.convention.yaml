rule_id: traceability.plan.executable-acceptance-has-test
statement: Every executable acceptance is bound by at least one Bun test; an acceptance is executable unless the one feature or train that owns it is planned.
metadata:
  disposition: strict
implementation:
  type: validator
  ref: atdd_traceability_closure
