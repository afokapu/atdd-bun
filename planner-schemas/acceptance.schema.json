{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Acceptance Schema (V2)",
  "description": "Validates acceptance criteria in WMBT files. Used by both planner (generates) and tester (consumes).",
  "$comment": "Two representations exist. (1) The STANDALONE shape (this root object) is strict — required [identity,given,when,then,signal,metadata] plus the §4.3 measurability anyOf below (harness OR signal.metric+threshold), additionalProperties:false. (2) The WMBT-EMBEDDED shape (definitions/embedded_acceptance) is lighter and is what real plan/ WMBT files carry inline: no top-level `signal`, `when` has only `abstract` (no `action`), `then` has only `abstract` (no `assertions`), `metadata` uses `author`/`created` (not `wagon`). The strict root object is NOT applied as a file gate to embedded acceptances (read literally it would reject every real WMBT file); embedded acceptances are validated by the Python content validators. Documented for honesty — no pass/fail change. See #760.",
  "type": "object",
  "required": ["identity", "given", "when", "then", "signal", "metadata"],
  "anyOf": [
    {"$comment": "Spec §4.3 measurability, as schema structure: EITHER harness.type OR (signal.metric AND signal.threshold). `harness` sat in `required` until #1925, which made the second half unauthorable — atdd author acceptance refused every metric-only acceptance the invariant explicitly admits.", "required": ["harness"]},
    {
      "required": ["signal"],
      "properties": {"signal": {"required": ["metric", "threshold"]}}
    }
  ],
  "additionalProperties": false,
  "properties": {
    "identity": {
      "type": "object",
      "required": ["urn", "id", "purpose", "phase"],
      "additionalProperties": false,
      "properties": {
        "urn": {
          "type": "string",
          "pattern": "^acc:(?:train:[a-z][a-z0-9-]*:[a-z][a-z0-9-]*:[a-z][a-z0-9-]*|[a-z0-9][a-z0-9-]*:(?:[DLPCEMYRK][0-9]{3}-(?:UNIT|HTTP|EVENT|WS|E2E|A11Y|VIS|METRIC|JOB|DB|SEC|LOAD|SCRIPT|WIDGET|GOLDEN|BLOC|INTEGRATION|RLS|EDGE|REALTIME|STORAGE|SMOKE)-[0-9]{3}(?:-[a-z0-9-]+)?|[a-z][a-z0-9-]*))$",
          "description": "Acceptance URN, BOTH shapes (spec v12 §3.3): wagon-parented acc:{wagon}:{wmbt_id}-{harness}-{NNN}[-{slug}], or train-parented acc:train:{subject}:{slug}:{acceptance-slug} (the #1421 typed train identity, admitted by #1548). MUST stay byte-identical to families.acc.pattern in src/atdd/coach/utils/graph/urn_grammar.yaml — the grammar is the single source and this copy exists only because JSON Schema cannot reference it. Pinned by test_acceptance_schema_urn_pattern_matches_the_grammar."
        },
        "id": {
          "type": "string",
          "pattern": "^AC-(HTTP|UNIT|EVENT|WS|E2E|A11Y|VISUAL|METRIC|JOB|DB|SEC|LOAD|SCRIPT|WIDGET|GOLDEN|BLOC|INTEGRATION|RLS|EDGE_FUNCTION|REALTIME|STORAGE|SMOKE)-[0-9]{3}$"
        },
        "purpose": {
          "type": "string",
          "minLength": 3
        },
        "phase": {
          "type": "string",
          "enum": ["RED", "GREEN", "SMOKE", "REFACTOR"]
        }
      }
    },

    "harness": {
      "type": "object",
      "required": ["type", "category"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["unit", "http", "event", "ws", "e2e", "a11y", "visual", "metric", "job", "db", "sec", "load", "script", "widget", "golden", "bloc", "integration", "rls", "edge_function", "realtime", "storage", "smoke"]
        },
        "category": {
          "type": "string",
          "enum": ["frontend", "backend", "integration"]
        },
        "language_hint": {
          "type": "string",
          "enum": ["dart", "typescript", "python", "go", "auto"]
        }
      }
    },

    "given": {
      "type": "object",
      "required": ["abstract"],
      "additionalProperties": false,
      "properties": {
        "abstract": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "Human-readable Gherkin Given statements for stakeholder readability"
        },
        "auth": {
          "type": "object",
          "properties": {
            "required": {"type": "boolean"},
            "type": {"type": "string"},
            "scopes": {"type": "array", "items": {"type": "string"}}
          }
        },
        "data": {
          "type": "array",
          "items": {"type": "object"}
        },
        "environment": {
          "type": "object"
        },
        "stubs": {
          "type": "object"
        },
        "custom": {
          "type": "object"
        }
      }
    },

    "when": {
      "type": "object",
      "required": ["abstract", "action"],
      "additionalProperties": false,
      "properties": {
        "abstract": {
          "type": "string",
          "minLength": 5,
          "description": "Human-readable Gherkin When statement for stakeholder readability"
        },
        "action": {
          "type": "string",
          "enum": ["request", "invoke", "emit", "connect", "navigate", "query", "measure", "render", "mutate"]
        },
        "target": {
          "type": "string"
        },
        "parameters": {
          "type": "object"
        }
      }
    },

    "then": {
      "type": "object",
      "required": ["abstract", "assertions"],
      "additionalProperties": false,
      "properties": {
        "abstract": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string"
          },
          "description": "Human-readable Gherkin Then statements for stakeholder readability"
        },
        "assertions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$comment": "`tolerance` was DROPPED here by #1957. It was declared as a bare {\"type\": \"object\"} — no inner shape, so two teams expressing the same band would write it differently and no gate could compare them — and it was carried by 0 of the 26 acceptances that declare `then.assertions` at all, with no reader anywhere in src/. The quantitative mechanism is `signal.metric` + `signal.threshold`, declared and wired by #1925 and resolved by runners/metric_runner.py; a second one on the assertion leg would split the grammar. Do not re-add a quantitative affordance here without a reader.",
            "type": "object",
            "required": ["type"],
            "properties": {
              "type": {
                "type": "string",
                "enum": ["equals", "contains", "matches", "exists", "count", "performance"]
              },
              "target": {"type": "string"},
              "value": {}
            }
          }
        },
        "count": {
          "type": "integer",
          "minimum": 1
        },
        "red_strategy": {
          "type": "string",
          "enum": ["not_implemented", "expect_false", "throw_error", "timeout"]
        }
      }
    },

    "signal": {
      "type": "object",
      "description": "Telemetry signals following OpenTelemetry (metrics, traces, logs) + analytics events, PLUS the executable enforcement bar (metric + threshold).",
      "$comment": "`signal` carries two distinct things and both are legal. (1) DESCRIPTIVE: `metrics`/`traces`/`logs`/`events` are the OTel signals this slice EMITS — consumed by observability, not by a gate. (2) EXECUTABLE: `metric` + `threshold` are the bar this acceptance IS JUDGED BY — resolved by runners/metric_runner.py to <repo>/.atdd/metrics/<metric>.py (or the toolkit commons) and evaluated as passes(compute(root), threshold). They are not rival spellings of one idea. Before #1925 only the descriptive half was declared, and `additionalProperties: false` therefore FORBADE the exact shape the runtime requires — see #1925.",
      "additionalProperties": false,
      "properties": {
        "metric": {
          "type": "string",
          "minLength": 1,
          "description": "Executable enforcement bar: the metric name the metric runner resolves to <repo>/.atdd/metrics/<name>.py or src/atdd/runners/metrics/<name>.py, each exporting compute(repo_root) and passes(value, threshold)."
        },
        "threshold": {
          "type": ["number", "boolean", "string"],
          "description": "Scalar `metric` is judged against, passed VERBATIM to the module's passes(value, threshold) — the authored type is part of the contract. `0` and `false` are meaningful thresholds, not absent ones."
        },
        "metrics": {
          "type": "array",
          "description": "OpenTelemetry metrics for observability",
          "items": {
            "$comment": "`threshold` was DE-REQUIRED here by #1957. This array is the DESCRIPTIVE half of `signal` — the OTel metrics a slice EMITS, consumed by observability and by no gate — so a bar does not belong to it: the executable bar is `signal.metric` + `signal.threshold` (#1925). Requiring `threshold` on every declared metric compelled every author to state a number nothing would ever evaluate, and 0 of 1347 acceptances use `signal.metrics[]` at all. It stays DECLARED as optional alerting metadata; what identifies a declared metric — `name` and `type` — stays required, and the item stays closed.",
            "type": "object",
            "required": ["name", "type"],
            "additionalProperties": false,
            "properties": {
              "name": {
                "type": "string",
                "pattern": "^telemetry:metric:(ui|ux|be|nw|db|st|tm|sc|au|fn|if):[a-z][a-z0-9-]*:[a-z][a-z0-9-]*(?:-[a-z][a-z0-9-]*)?:(latency|duration|throughput|error_rate|success_rate|count|size|age|staleness|freshness)$",
                "description": "Metric URN: telemetry:metric:{plane}:{domain}:{resource}:{measure}. Artifact (domain:resource) must exist in contracts/_artifacts.yaml. No operation/wagon names in URN."
              },
              "type": {
                "type": "string",
                "enum": ["counter", "gauge", "histogram", "summary"],
                "description": "OpenTelemetry metric instrument type"
              },
              "unit": {
                "type": "string",
                "description": "Unit (ms, s, bytes, percent, count) per OpenTelemetry semantic conventions"
              },
              "description": {
                "type": "string",
                "description": "Human-readable metric description"
              },
              "aggregation": {
                "type": "string",
                "enum": ["p50", "p95", "p99", "mean", "sum", "count", "rate", "max", "min"],
                "description": "Aggregation method for metric (query-time, not OTel core but practical)"
              },
              "threshold": {
                "oneOf": [
                  {"type": "number"},
                  {"type": "string", "pattern": "^[0-9]+(ms|s|%|MB|GB|bytes)$"}
                ],
                "description": "OPTIONAL expected threshold for alerting/dashboards. Descriptive only — no gate reads it; the executable bar an acceptance is judged by is the sibling `signal.metric` + `signal.threshold` (#1925). De-required by #1957."
              },
              "attributes": {
                "type": "array",
                "items": {"type": "string"},
                "description": "Metric attributes/dimensions (OTel terminology)"
              }
            }
          }
        },
        "traces": {
          "type": "array",
          "description": "OpenTelemetry trace spans expected during execution",
          "items": {
            "type": "object",
            "required": ["name"],
            "additionalProperties": false,
            "properties": {
              "name": {
                "type": "string",
                "pattern": "^telemetry:trace:(ui|ux|be|nw|db|st|tm|sc|au|fn|if):[a-z][a-z0-9-]*:[a-z][a-z0-9-]*(?:-[a-z][a-z0-9-]*)?$",
                "description": "Trace URN: telemetry:trace:{plane}:{domain}:{resource}. Artifact must exist in contracts/_artifacts.yaml. No measure segment for traces."
              },
              "kind": {
                "type": "string",
                "enum": ["server", "client", "producer", "consumer", "internal"],
                "description": "Span kind per OpenTelemetry spec"
              },
              "attributes": {
                "type": "array",
                "items": {"type": "string"},
                "description": "Required span attributes"
              },
              "parent": {
                "type": "string",
                "description": "Parent span name (for nested spans)"
              }
            }
          }
        },
        "logs": {
          "type": "array",
          "description": "OpenTelemetry log records expected during execution",
          "items": {
            "type": "object",
            "required": ["body"],
            "additionalProperties": false,
            "properties": {
              "body": {
                "type": "string",
                "description": "Log message body (can include {placeholders})"
              },
              "severity": {
                "type": "string",
                "enum": ["trace", "debug", "info", "warn", "error", "fatal"],
                "description": "Log severity per OpenTelemetry spec"
              },
              "attributes": {
                "type": "object",
                "description": "Structured log attributes"
              },
              "name": {
                "type": "string",
                "pattern": "^telemetry:log:(ui|ux|be|nw|db|st|tm|sc|au|fn|if):[a-z][a-z0-9-]*:[a-z][a-z0-9-]*(?:-[a-z][a-z0-9-]*)?$",
                "description": "Log URN (optional): telemetry:log:{plane}:{domain}:{resource}. Artifact must exist in contracts/_artifacts.yaml."
              }
            }
          }
        },
        "events": {
          "type": "array",
          "description": "Analytics events (product tracking via Segment/Mixpanel/Amplitude)",
          "items": {
            "type": "object",
            "required": ["name"],
            "additionalProperties": false,
            "properties": {
              "name": {
                "oneOf": [
                  {
                    "type": "string",
                    "pattern": "^telemetry:event:(ui|ux|be|nw|db|st|tm|sc|au|fn|if):[a-z][a-z0-9-]*:[a-z][a-z0-9-]*(?:-[a-z][a-z0-9-]*)?$",
                    "description": "Event URN: telemetry:event:{plane}:{domain}:{resource}. Resource must be past-tense verb from lexicon."
                  },
                  {
                    "type": "string",
                    "pattern": "^[A-Z][a-zA-Z0-9 ]*$",
                    "description": "Analytics event name (e.g., 'Order Created')"
                  }
                ],
                "description": "Event name: telemetry URN for OTel events, or analytics convention for product events"
              },
              "properties": {
                "type": "array",
                "items": {"type": "string"},
                "description": "Event properties to track"
              },
              "category": {
                "type": "string",
                "description": "Event category (conversion, engagement, etc.)"
              },
              "tracking_plan_ref": {
                "type": "string",
                "description": "Reference to analytics tracking plan"
              }
            }
          }
        }
      }
    },

    "metadata": {
      "type": "object",
      "required": ["wagon"],
      "additionalProperties": false,
      "properties": {
        "wagon": {"type": "string"},
        "feature": {"type": "string"},
        "route": {
          "type": "object",
          "properties": {
            "layers": {"type": "array", "items": {"type": "string"}},
            "sequence": {"type": "array", "items": {"type": "string"}}
          }
        },
        "tags": {
          "type": "array",
          "items": {"type": "string"}
        }
      }
    },

    "appendices": {
      "type": "array",
      "description": "Supplementary files for this acceptance criterion",
      "items": {
        "$ref": "appendix.schema.json#/definitions/appendix_reference"
      }
    }
  },
  "definitions": {
    "embedded_acceptance": {
      "type": "object",
      "description": "The lighter acceptance shape embedded inside WMBT files (plan/<wagon>/<CODE>.yaml acceptances[]). Reuses the standalone identity/harness/given shapes; when/then carry only `abstract`; metadata uses author/created. NOT applied as a file gate — documented for honesty (#760).",
      "required": ["identity", "given", "when", "then"],
      "anyOf": [
        {"$comment": "Spec §4.3 measurability — see the root shape's anyOf (#1925).", "required": ["harness"]},
        {
          "required": ["signal"],
          "properties": {"signal": {"required": ["metric", "threshold"]}}
        }
      ],
      "additionalProperties": true,
      "properties": {
        "identity": {"$ref": "#/properties/identity"},
        "harness": {"$ref": "#/properties/harness"},
        "given": {"$ref": "#/properties/given"},
        "when": {
          "type": "object",
          "required": ["abstract"],
          "properties": {
            "abstract": {"type": "string", "minLength": 5}
          }
        },
        "then": {
          "type": "object",
          "required": ["abstract"],
          "properties": {
            "abstract": {"type": "array", "minItems": 1, "items": {"type": "string"}}
          }
        },
        "metadata": {
          "type": "object",
          "properties": {
            "author": {"type": "string"},
            "created": {"type": "string"}
          }
        }
      }
    }
  }
}